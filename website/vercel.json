{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "bun run css:build && bunx vite build",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/contact", "destination": "/api/contact" }
  ]
}
